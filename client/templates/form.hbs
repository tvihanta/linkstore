    <div class="editor"s> 
        <input id="url-input" type="url" name="linkUrl" placeholder="url" value="{{this.url}}"/>
        <button id="save" data-id="{{this.id}}" class="btn">Save</button>
        <br/>
        <div>
         <input type="text" id="title" name="linkTitle" placeholder="title" value="{{this.title}}" />
        <input type="text" name="linkTags"  id="tags" placeholder="tags" value="{{this.tags}}" />
        </div>
    </div>
